package com.nextstep.views.temp;

import com.vaadin.flow.router.Route;

@Route("perfil")
public class PerfilView {
    // Vista de perfil vacía para el momento (solo constructor)
    public PerfilView() {
    }
}